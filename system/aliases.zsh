# for open files directly
alias -s gz='tar xzvf'
alias -s tgz='tar xzvf'
alias -s zip='unzip'
alias -s bz2='tar xjvf'
alias -s apk='adb install -r'

# convenient for editing configs
alias ev='vim ~/.vimrc'
alias ez='vim ~/.zshrc'
alias sz='source ~/.zshrc'
